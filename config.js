const GAME_CONFIG = {
    CANVAS_WIDTH: 1024, //windowWidth; 
    CANVAS_HEIGHT: 800, //windowHeight;
    PL_DEFAULT_SIZE: 40,
    PL_DEFAULT_SPEED: 3,
    BLT_SPEED: 3,
    BLT_POOL: 20,
    BLT_COOLDOWN: 2,
    BLT_SIZE: 20,
    SERVER_TICK: 60
}

module.exports = {
    GC: GAME_CONFIG
}